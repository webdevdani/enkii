import React from 'react';
import PropTypes from 'prop-types';

const SignUp = (props) => {
    return (
        <div>
            Sign Up
        </div>
    );
}

SignUp.propTypes = {

};

SignUp.defaultProps = {

};

export default SignUp;
