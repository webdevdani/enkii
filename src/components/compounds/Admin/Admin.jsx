import React from 'react';
import PropTypes from 'prop-types';

const Admin = (props) => {
    return (
        <div>
            Admin
        </div>
    );
}

Admin.propTypes = {

};

Admin.defaultProps = {

};

export default Admin;
