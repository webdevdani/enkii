import React from 'react';
import PropTypes from 'prop-types';

const Account = (props) => {
    return (
        <div>
            Account
        </div>
    );
}

Account.propTypes = {

};

Account.defaultProps = {

};

export default Account;
