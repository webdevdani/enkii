import React from 'react';
import PropTypes from 'prop-types';

const Landing = (props) => {
    return (
        <div>
            Landing
        </div>
    );
}

Landing.propTypes = {

};

Landing.defaultProps = {

};

export default Landing;
