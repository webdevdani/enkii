const baseInputStyles = (props) => `
    padding: ${props.theme.paddingS};
    border-radius: ${props.theme.borderRadius};
    border: ${props.theme.baseBorder};
    font-size: 1rem;
    width: 100%;
`;

export default baseInputStyles;
