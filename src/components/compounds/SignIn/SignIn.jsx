import React from 'react';
import PropTypes from 'prop-types';

const SignIn = (props) => {
    return (
        <div>
            Sign In
        </div>
    );
}

SignIn.propTypes = {

};

SignIn.defaultProps = {

};

export default SignIn;
