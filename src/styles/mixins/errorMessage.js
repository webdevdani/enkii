const errorMessage = (props) => `
    color: ${props.theme.errorColor};
    font-weight: 600;
    font-size: 0.5rem;
    margin-bottom: ${props.theme.paddingXs};
`;

// TO-DO: Add slide in animation

export default errorMessage;
